"use client";
import { useState } from "react";
import { courses } from "@/data/courses";
import { CourseCard } from "@/components/CourseCard";
const filters=["All","Data Analytics","Accounting","Payroll","Computer Skills"];
export default function CoursesPage(){const [active,setActive]=useState("All"); const shown=active==="All"?courses:courses.filter(c=>c.category===active); return <main id="main"><section className="page-hero courses-hero"><span className="eyebrow">Professional programmes</span><h1>Skills you can<br/>put to work.</h1><p>Browse practical, instructor-led training available online and physically in Harare. Every programme combines clear instruction with hands-on exercises.</p><div className="programme-facts"><span>Instructor-led learning</span><span>Practical assignments</span><span>Eligible certificates</span></div></section><section className="section course-browser"><div className="catalogue-head"><div className="filters" role="group" aria-label="Filter courses">{filters.map(f=><button key={f} className={active===f?"active":""} onClick={()=>setActive(f)}>{f}</button>)}</div><p>{shown.length} programmes</p></div><div className="course-list">{shown.map(c=><CourseCard key={c.slug} course={c}/>)}</div></section></main>}
